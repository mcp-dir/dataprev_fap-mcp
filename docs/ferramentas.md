# Ferramentas

Dataprev: Fator Acidentário de Prevenção (FAP) expõe 1 ferramenta (todas somente leitura).

### 1. `dataprev_fap_consultar`
**Input**: `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `ano_vigencia` (opcional), `cnpj_estabelecimento`

Dataprev: Fator Acidentário de Prevenção (FAP), consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Dataprev: Fator Acidentário de Prevenção (FAP)
```
